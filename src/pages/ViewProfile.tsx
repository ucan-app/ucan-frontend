import React from "react";

/*type ViewProfileProps = {
  user: User;
};

type ViewProfileState = {};
*/

// Define the component with explicit return type
const ViewProf: React.FC<{}> = () => {
  return (
    <div>
      <h1>ViewProf Page</h1>
    </div>
  );
};

export default ViewProf;
